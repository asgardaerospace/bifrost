fastapi>=0.115.0
uvicorn[standard]>=0.30.6
sqlalchemy>=2.0.35
alembic>=1.13.3
psycopg[binary]>=3.2.10
pydantic>=2.9.2
pydantic-settings>=2.5.2
python-dotenv>=1.0.1
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# bcrypt 4.x changed `__about__` removal causing passlib detection warnings.
# 4.0.1 is the last version passlib 1.7.x natively recognizes. The
# `_truncate_for_bcrypt` shim in core/security.py keeps us safe across versions.
bcrypt>=4.0,<5.0

# Realtime fanout / pub-sub. Optional at runtime — health check probes it
# when REDIS_URL is set, but the app degrades cleanly when unreachable.
redis>=5.0,<6.0

# Test deps (Sprint 0 smoke harness).
pytest>=8.0.0
httpx>=0.27.0
